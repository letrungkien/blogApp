<% provide(:title, @entry.title) %>
<div class="row">
  <aside class="span4">
    <section>
      <h1>
        <%= gravatar_for @user %>
        <%= link_to @user.name, @user %>
      </h1>
    </section>
    <section>
      <%= render 'shared/stats' %>
    </section>
  </aside>
  <div class="span8">
    <% unless @entry.nil? %>
      <h3>
        <%= @entry.title %>
      </h3>
      <ol class="entries">
        <%= render 'entries/entry_content', entry: @entry %>
      </ol>
    <% end %>
    <%= render 'entries/entry_comment_input' %>
  </div>
</div>